picom-animations-git no longer exists so it has been replaced, it works fine one one laptop but very slow on the other

To do

- create python config so user can select if they want animations or not

- attempt to merge most features from shell scripts into python
